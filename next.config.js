/** @type {import('next').NextConfig} */
// This file configures Next.js settings
const nextConfig = {
  reactStrictMode: true,
  // Enable image optimization
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig

